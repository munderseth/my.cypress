{ "name": "my.cypress",
  "version": ".0.1",
  "description": "This is a sandbox to learn about cypress.",
  "dependencies": {
    "cypress": "^12.6.0"
  },
  "scripts": {
    "test": "npx cypress run",
    "lint": "eslint src/*.js"
  },
  "devDependencies": {
    "cypress-terminal-report": "^5.0.2",
    "eslint": "^8.35.0"
  }
}
